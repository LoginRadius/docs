With [Loginradius CLI](https://github.com/LoginRadius/lr-cli/), You can delete users from CLI using the simple command below

   ```sh
   $ lr delete account --email <email> (or) --uid <uid>
		User account sucessfully deleted
   ```
