With [Loginradius CLI](https://github.com/LoginRadius/lr-cli/), You can reset password from CLI using the simple command below

   ```sh
   $ lr set account-password --uid <uid> --password <password>
		New password hash is: <hash>
   ```